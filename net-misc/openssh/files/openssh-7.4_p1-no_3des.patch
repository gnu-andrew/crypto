--- a/cipher.c.old	2016-12-19 04:59:41.000000000 +0000
+++ b/cipher.c	2017-01-24 00:50:12.949940003 +0000
@@ -53,9 +53,11 @@
 
 #ifdef WITH_SSH1
 extern const EVP_CIPHER *evp_ssh1_bf(void);
+#ifndef OPENSSL_NO_DES
 extern const EVP_CIPHER *evp_ssh1_3des(void);
 extern int ssh1_3des_iv(EVP_CIPHER_CTX *, int, u_char *, int);
 #endif
+#endif
 
 struct sshcipher_ctx {
 	int	plaintext;
@@ -88,15 +90,19 @@
 
 static const struct sshcipher ciphers[] = {
 #ifdef WITH_SSH1
+#ifndef OPENSSL_NO_DES
 	{ "des",	SSH_CIPHER_DES, 8, 8, 0, 0, 0, 1, EVP_des_cbc },
 	{ "3des",	SSH_CIPHER_3DES, 8, 16, 0, 0, 0, 1, evp_ssh1_3des },
+#endif /* OPENSSL_NO_DES */
 # ifndef OPENSSL_NO_BF
 	{ "blowfish",	SSH_CIPHER_BLOWFISH, 8, 32, 0, 0, 0, 1, evp_ssh1_bf },
 # endif /* OPENSSL_NO_BF */
 #endif /* WITH_SSH1 */
 #ifdef WITH_OPENSSL
 	{ "none",	SSH_CIPHER_NONE, 8, 0, 0, 0, 0, 0, EVP_enc_null },
+#ifndef OPENSSL_NO_DES
 	{ "3des-cbc",	SSH_CIPHER_SSH2, 8, 24, 0, 0, 0, 1, EVP_des_ede3_cbc },
+#endif
 # ifndef OPENSSL_NO_BF
 	{ "blowfish-cbc",
 			SSH_CIPHER_SSH2, 8, 16, 0, 0, 0, 1, EVP_bf_cbc },
@@ -629,9 +635,11 @@
 		break;
 #endif
 #ifdef WITH_SSH1
+#ifndef OPENSSL_NO_DES
 	case SSH_CIPHER_3DES:
 		return ssh1_3des_iv(cc->evp, 0, iv, 24);
 #endif
+#endif
 	default:
 		return SSH_ERR_INVALID_ARGUMENT;
 	}
@@ -675,9 +683,11 @@
 		break;
 #endif
 #ifdef WITH_SSH1
+#ifndef OPENSSL_NO_DES
 	case SSH_CIPHER_3DES:
 		return ssh1_3des_iv(cc->evp, 1, (u_char *)iv, 24);
 #endif
+#endif
 	default:
 		return SSH_ERR_INVALID_ARGUMENT;
 	}
--- a/cipher-3des1.c.old	2016-12-19 04:59:41.000000000 +0000
+++ b/cipher-3des1.c	2017-01-24 00:47:51.808268861 +0000
@@ -28,6 +28,8 @@
 
 #include "ssherr.h"
 
+#ifndef OPENSSL_NO_DES
+
 /*
  * This is used by SSH1:
  *
@@ -155,4 +157,6 @@
 	ssh1_3des.flags = EVP_CIPH_CBC_MODE | EVP_CIPH_VARIABLE_LENGTH;
 	return &ssh1_3des;
 }
+
+#endif /* OPENSSL_NO_DES */
 #endif /* WITH_SSH1 */
